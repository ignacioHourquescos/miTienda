

///////////DESAFIO CLASE 5- entrega 16/11
  /containers/Home/Home.js pueden tener una función getProducts() en el mismo archivo pero fuera del componente
   Home que haga el setTimeout de 2segs y  luego devuelva la promesa con el array de productos harcodeados.

  En el componente Home del mismo archivo consumen la promesa de getProducts().
  
  Usan en el jsx el componente ItemList y le envían por props los productos.
- /components/ItemList/ItemList.js hacen un foreach/map del los datos que reciben por 
props y por cada elemento llaman al componente Item
 enviándole por props los datos de c/producto.
- /components/Item/Item.js recibe por props el detalle de un producto en particular y 
renderean una card o similar con la info del producto.